PB_ADMIN_EMAIL=portfolio@example.com
PB_ADMIN_PASSWORD=changeme1

# PocketBase URL for server-side API calls (Docker internal network, etc.)
POCKETBASE_INTERNAL_URL=http://pocketbase:8090

# Override PocketBase public URL. If unset, file URLs are relative (resolved by browser).
# Only needed if PocketBase is on a different domain than the Next.js app.
# NEXT_PUBLIC_POCKETBASE_URL=http://localhost:8090

# Public site URL – used for metadataBase (OG tags, canonical URLs, etc.)
# NEXT_PUBLIC_SITE_URL=https://example.com

NODE_ENV=production
